import { StyledFooter } from "./styles/Footer.styled";

const Footer = () => {
  return (
    <StyledFooter>
      <input type="text" placeholder="What we have to do?" />
      <a href="#">Add</a>
    </StyledFooter>
  );
};

export default Footer;
